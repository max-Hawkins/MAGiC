# cv2 = pyimport("cv2")
# np = pyimport("numpy")

# "Implement contrast limiting adaptive histogram equalization
#     on a 256x256 power spectrogram."
# function clahe(power_spec)
#     clahe_f = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8,8))
#     power_spec_clahe = clahe_f.apply(power_spec)
#     return power_spec_clahe
# end

# function pulse_search()

# end

# Implement CLAHE and Tensorflow models here
# assumed passed large averaged power array from julia